#include "lists.h"
/**
 * is_palindrome - checks if a singly linked list is a palindrome.
 * @head: pointer to pointer of first node of listint_t list
 * Return: 1 if list is a palindrome 0 if it's not.
 */
int is_palindrome(listint_t **head)
{
